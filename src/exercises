//Ex. my_htons write a function that change order of bytes (from LE to BE)
/*
 * How strlen can be implemented
 * for(size=0; request[size]; size++);
*/
/*
 * Alternative way to write line 59
 * for(size=0; (t=read(s, response+size, 1000000-size)>0; size=size+t);
*/
/**
 *  response[t] = 0;
 *
 *  for(i=0; response[i]; i++)
 *      printf("%c", response[i]);
 */
